<% provide :title, 'All users' %>
<div class="page-header">
  <h1>All users <small>who are all these people?</small></h1>
</div>

<ul class="users unstyled">
  <%= render @users %> <!-- infers _user partial to iterate @users as a list of User objects -->
</ul>

<%= page_navigation_links @users %>